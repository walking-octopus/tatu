/// Low-level cryptographic primitives.
///
/// This module contains VDF and other primitives that should generally not be used directly.
/// Use the higher-level `identity` and `protocol` modules instead.
pub(crate) mod vdf;
